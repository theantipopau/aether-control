<UserControl
    x:Class="AetherControl.App.Controls.PortraitMetricTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource PortraitCardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="11" FontWeight="SemiBold"
                       Foreground="{StaticResource PortraitTextSecondaryBrush}" Margin="0,0,0,4" />

            <TextBlock x:Name="ValueText" Grid.Row="1" FontFamily="Cascadia Mono, Consolas" FontSize="26" FontWeight="Bold"
                       TextTrimming="CharacterEllipsis" />

            <Grid x:Name="BarTrack" Grid.Row="2" Height="5" Margin="0,8,0,0" CornerRadius="3"
                  Background="{StaticResource PortraitCardBorderBrush}" SizeChanged="OnBarTrackSizeChanged">
                <Border x:Name="BarIndicator" HorizontalAlignment="Left" CornerRadius="3" Width="0" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
